# hashIdentifier
Identify password hashes with this python script right on yo terminal

# Usage
python hashidn.py 'your hash here'

# The script can identify common password hashes like MD5, SHA-1, SHA-256, SHA-512, NTLM, MD4, RIPEMD, BLAKE2, SHA-3, etc.
